%ul#companies
  %li.company{'ng-repeat' => 'company in companies'}
    .logo
      %img{'ng-src' => '{{ company.logo_url }}'}

    .info
      .name {{ company.name }}
      .slogan {{ company.slogan }}
